id: boundary-check-001
repository: .txagent-fixtures/order-service
base_commit: 9aad50b347d9c735fdb1e0c75bc743bec6b3595c
issue: >-
  Orders created exactly at the cutoff time are rejected. Fix the boundary handling
  without changing behavior before or after the cutoff.
verification:
  command: python3 -m unittest discover -s tests
  timeout_seconds: 120
budget:
  max_llm_calls: 40
  max_tool_calls: 80
