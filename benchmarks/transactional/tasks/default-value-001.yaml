id: default-value-001
repository: fixtures/config-service
base_commit: c2904ad
issue: Missing retry configuration uses the wrong default value.
verification:
  command: uv run pytest -q
  timeout_seconds: 120
budget:
  max_llm_calls: 20
  max_tool_calls: 80
